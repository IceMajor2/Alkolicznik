# database config
spring.datasource.driver-class-name=org.hsqldb.jdbc.JDBCDriver
spring.datasource.url=jdbc:hsqldb:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=sa
spring.jpa.hibernate.ddl-auto=none

spring.main.allow-bean-definition-overriding=true
server.error.include-message=always
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# disable initialization of production data
spring.sql.init.mode=never
command.line.runner.enabled=false

# print sql queries
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

jwt.key=0e35647d64c1589917ac4affe1eba3bcbdec20f1068b7e94e020479e9f9c7923
# JWT secret key is already provided by default above, however you may choose to generate the key yourself.
# For example, you can generate it here (IMPORTANT: choose 256-bit as key size): https://asecuritysite.com/encryption/plain